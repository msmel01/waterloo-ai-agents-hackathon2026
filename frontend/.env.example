# Backend API URL (no trailing slash, include host only)
# OpenAPI paths already include /api/v1
VITE_API_URL=http://localhost:8000

# Public heart slug to show in the frontend
VITE_HEART_SLUG=heart

# Clerk publishable key (from Clerk Dashboard > API Keys)
# Required for auth. Use the same value as CLERK_PUBLISHABLE_KEY; Vite requires VITE_ prefix for client access.
VITE_CLERK_PUBLISHABLE_KEY=
