# LiveKit server URL (e.g. wss://your-project.livekit.cloud)
# Required for connecting to the voice room.
# Set this in Vercel project settings for production.
VITE_LIVEKIT_URL=

# Clerk publishable key (from Clerk Dashboard > API Keys)
# Required for auth. Add when backend is connected; app runs without it but auth won't work.
VITE_CLERK_PUBLISHABLE_KEY=
